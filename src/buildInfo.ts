/**
 * buildInfo.ts — Single source of truth for the build number.
 *
 * Agents: increment BUILD_NUMBER by 1 for every implemented change or pull request.
 * See AGENTS.md for the full workflow requirement.
 */

/** Current build number, shown in the Settings tab footer. */
export const BUILD_NUMBER = 348;
